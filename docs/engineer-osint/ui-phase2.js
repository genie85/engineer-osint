(function(){const D=window.__ENGINEER_DATA__;if(!D)return;const R=D.records?.records||[],L=D.leads?.leads||[],rel=D.relations?.relations||[];const rm=new Map(R.map(x=>[x.id,x]));const all=[...R,...L];const lang=()=>window.ENGINEER_I18N?.getLanguage?.()||'cs';const pick=(r,k)=>window.ENGINEER_I18N?.pick?.(r,k)??r?.[k]??'';const nav=document.querySelector('#sidebar nav');const btn=(id,t,fn)=>{if(!nav||document.getElementById(id))return;const b=document.createElement('button');b.id=id;b.type='button';b.textContent=t;b.style.cssText='display:block;width:100%;text-align:left;border:0;background:transparent;color:#91a3b8;padding:10px 12px;border-radius:9px';b.onclick=fn;nav.appendChild(b)};function related(id){const s=new Set();for(const x of rel){if(x.from===id)s.add(x.to);if(x.to===id)s.add(x.from)}return[...s]}function watch(){const v=document.getElementById('view');if(!v)return;document.getElementById('pageTitle').textContent='Watchlist Health';v.innerHTML='<section class="card section"><h2>Watchlist Health</h2><p class="muted">Heuristika intenzity hledání; nevyjadřuje pravdivost ani uzavření leadu.</p>'+L.map(x=>'<article class="item"><strong>'+x.id+' — '+(pick(x,'title')||x.topic||'')+'</strong><div class="muted">'+(x.status||'OPEN')+' · attempts '+(x.attempts||0)+'</div><div>'+(x.note||x.next_action||'')+'</div></article>').join('')+'</section>'}btn('engineerWatchBtn','Watchlist Health',watch);const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function timeline(){const d=document.getElementById('detailContent');if(!d||d.querySelector('.engineer-timeline'))return;const txt=d.textContent||'';const m=txt.match(/ENG-(?:TECH|UNIT|EVT|DOC|TTP|SIG)-\d+/);if(!m)return;const r=rm.get(m[0]);if(!r)return;let obs=[...(r.temporal_observations||r.timeline_events||[])];if(!obs.length&&(r.date||r.event_date||r.publication_date))obs.push({event_date:r.event_date||r.date||r.publication_date,summary:'Source/event observation',temporal_status:r.temporal_status||'DATE_UNCERTAIN'});obs=obs.sort((a,b)=>String(a.event_date||a.event_date_from||a.date||'').localeCompare(String(b.event_date||b.event_date_from||b.date||'')));const status=r.temporal_status||'',valid=[r.valid_from,r.valid_to].filter(Boolean).join(' → ');const currentGap=r.current_confirmation_gap?' · CURRENT CONFIRMATION GAP':'';const rows=obs.map(x=>{const dt=x.event_date||x.event_date_from||x.date||'DATE UNKNOWN',st=x.temporal_status||'DATE_UNCERTAIN',sum=(lang()==='cs'?(x.summary_cs||x.summary||x.summary_en):(x.summary_en||x.summary||x.summary_cs))||x.description||x.observation_type||x.type||'observation';return '<div class="item"><strong>'+esc(dt)+'</strong> <span class="badge">'+esc(st)+'</span><div>'+esc(sum)+'</div></div>'});if(r.run_id)rows.push('<div class="muted">Materialized: '+esc(r.run_id)+'</div>');if(r.last_update_run)rows.push('<div class="muted">Last update: '+esc(r.last_update_run)+'</div>');d.insertAdjacentHTML('beforeend','<section class="engineer-timeline card section"><h4>TIMELINE / VÝVOJ</h4><p class="muted">Časová aktuálnost je oddělena od confidence a kvality zdroje. Historický údaj není automaticky méně spolehlivý, ale bez nového potvrzení nedokládá současný stav.</p>'+(status||valid?'<div><b>Temporal status:</b> '+esc(status||'UNKNOWN')+(valid?' · valid '+esc(valid):'')+esc(currentGap)+'</div>':'')+rows.join('')+'</section>')}new MutationObserver(timeline).observe(document.getElementById('detailContent')||document.body,{childList:true,subtree:true});const search=document.getElementById('searchInput');if(search){search.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();if(q.length<2)return;const hits=all.filter(x=>JSON.stringify(x).toLowerCase().includes(q)).slice(0,10);let p=document.getElementById('engineerGraphResults');if(!p){p=document.createElement('div');p.id='engineerGraphResults';search.parentElement?.after(p)}p.className='card section';p.innerHTML='<b>Graph-aware search</b>'+hits.map(x=>'<div class="item" data-open="'+x.id+'">'+x.id+' — '+(pick(x,'title')||x.topic||'')+'</div>').join('');if(typeof window.openDetail==='function')p.querySelectorAll('[data-open]').forEach(e=>e.onclick=()=>window.openDetail(e.dataset.open))})}})();